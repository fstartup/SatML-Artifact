# Scenario A implementation

This module is the first executable laboratory for the paper's architecture. It intentionally begins with scripted agents so that failures in the harness itself are separated from failures caused by an LLM.

The P2 control boundary is deterministic and denies refund/account-change actions even when the agent proposes them.
